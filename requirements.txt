# Core dependencies
numpy>=1.20.0

# Optional dependencies for visualization
matplotlib>=3.3.0  # For real-time visualization dashboard

# Optional dependencies for text mode
transformers>=4.30.0  # For real tokenization in text mode

